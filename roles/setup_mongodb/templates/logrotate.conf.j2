#/etc/logrotate.d/
/var/log/mongodb/{{ env }}/*.log {
daily
rotate 5
compress
delaycompress
dateext
missingok
notifempty
sharedscripts
copytruncate
postrotate
/bin/kill -SIGUSR1 `cat /var/run/mongodb/{{ env }}/*.pid 2> /dev/null|xargs` 2> /dev/null || true
endscript
}
