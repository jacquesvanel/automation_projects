---
################################  testing  ################################
- name: set facts
  import_tasks: set_fact.yml
  tags:
    - set_facts 

- name: test task
  import_tasks: test.yml
  tags:
    - test
    - never

##########start of config##################################################
- name: add mongo service file
  import_tasks: mongo_service.yml
  tags:
    - reinstall
    - mongo_service

- name: add configsvr config/service files 
  include_tasks: no_sec_config_servers.yml
  when: "ansible_hostname in [configsvr_rsa, configsvr_rsb, configsvr_rsc]"
  tags:
    - reinstall
    - no_sec_config_servers

- name: add shardsvr config/service files
  include_tasks: no_sec_shard_servers.yml
  when: "ansible_hostname in [shard1a, shard1b, shard1c, shard2a, shard2b, shard2c, shard3a, shard3b, shard3c]"
  tags:
    - reinstall
    - no_sec_shard_servers

- name: add single_repset_svr config/service files
  include_tasks: no_sec_single_repset_servers.yml
  when: "ansible_hostname in [rs_1, rs_2, rs_3]" 
  tags:
    - reinstall
    - no_sec_single_repset_servers

- name: add mongos config/service files
  include_tasks: mongos.yml
  when: "ansible_hostname in [configsvr_rsa, configsvr_rsb, configsvr_rsc]"
  tags: 
    - reinstall
    - mongos

- name: setup users
  include_tasks: setup_users.yml
  when: "ansible_hostname in [configsvr_rsa, shard1a, shard2a, shard3a, rs_1]"
  tags: 
    - reinstall
    - setup_users

- name: create role
  include_tasks: create_role.yml
  tags: 
    - reinstall
    - create_role
    
- name: change ldap interval
  include_tasks: ldap_interval.yml
  tags:
    - reinstall
    - ldap_interval  

- name: start mongos
  include_tasks: start_mongos.yml
  when: "ansible_hostname in [configsvr_rsa, configsvr_rsb, configsvr_rsc]"
  tags:
    - reinstall
    - start_mongos

      #- name: add configsvr config/service files
      #  import_tasks: servers.yml
      #  tags:
      #    - reinstall
      #    - servers

- name: add configsvr config/service files
  include_tasks: config_servers.yml
  when: "ansible_hostname in [configsvr_rsa, configsvr_rsb, configsvr_rsc]"
  tags:
    - reinstall
    - config_servers

- name: add shardsvr config/service files
  include_tasks: shard_servers.yml
  when: "ansible_hostname in [shard1a, shard1b, shard1c, shard2a, shard2b, shard2c, shard3a, shard3b, shard3c]"
  tags:
    - reinstall
    - shard_servers

- name: add single_repset_svr config/service files
  include_tasks: single_repset_servers.yml
  when: "ansible_hostname in [rs_1, rs_2, rs_3]"
  tags:
    - reinstall
    - single_repset_servers

- name: initiate clusters
  include_tasks: initiate_clusters.yml
  tags:
    - reinstall
    - initiate_clusters

- name: add keep alive line to sysctl.conf
  include_tasks: add_line_to_sys.yml
  tags:
    - reinstall
    - keepalive

- name: add shards to cluster
  include_tasks: add_shards.yml
  tags:
    - reinstall
    - add_shards

- name: start mongo
  systemd:
    name: mongo
    state: restarted

- name: mms agent
  include_tasks: mms_agent.yml
  tags:
    - reinstall
    - mms_agent
#####################################################################
