---
#- set_fact:
#    vgs_name: "{{ item.key }}"
#  with_dict: "{{ ansible_lvm.vgs }}"

#- debug:
#    var: vgs_name

- name: Extend Data logical volume
  lvol:
    vg: "{{ vgs_name }}"
    lv: data
    size: +100%FREE
    resizefs: true
  ignore_errors: yes

