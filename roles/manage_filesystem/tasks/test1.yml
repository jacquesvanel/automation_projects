---
- debug:
    msg: 
      - "{{ item.value }}"
      - "{{ ansible_lvm.lvs.opt }}"  
    #is {{ item.value.size_g }}"
  with_dict: "{{ ansible_lvm.lvs.opt }}"
  #  when: item.key == "opt"
  
  #- debug:
  #    msg: "{{ ansible_devices }}"


