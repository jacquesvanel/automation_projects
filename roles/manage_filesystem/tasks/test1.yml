---
- name: get vgs
  debug:
    msg: "{{ item.key }}"
  with_dict: "{{ ansible_lvm.vgs }}"
  register: vg_name
- debug:
    msg: "{{ item.key }}"
  with_dict: "{{ ansible_lvm.lvs }}"

- debug:
    msg: "{{ ansible_devices }}"


