---
- name: get vgs
  debug:
    msg: "{{ item.key }}"
  with_dict: "{{ ansible_lvm.vgs }}"
  register: vg_name

- debug:
    msg: "{{ item.value.size_g }}"
  with_dict: "{{ ansible_lvm.lvs.opt }}"
  
- debug:
    msg: "{{ ansible_devices }}"


