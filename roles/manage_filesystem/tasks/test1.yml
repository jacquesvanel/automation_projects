---
- name: get app name
  shell: cat /etc/serverinfo.conf | grep IrsApplicationName=SADI | sed 's/IrsApplicationName=//g'
  args:
    warn: no
  register: app_name

- debug:
    var: app_name.stdout

- name: get env
  shell: cat /etc/serverinfo.conf | grep Environment=DSIT | sed 's/Environment=//g'
  args:
    warn: no
  register: env

- debug:
    var: env.stdout
#- debug:
#    msg: "{{ ansible_facts }}"
