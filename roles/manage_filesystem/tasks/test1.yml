---
- debug:
    msg: "{{ item.value.size_g }}" 
    #is {{ item.value.size_g }}"
  with_dict: "{{ ansible_lvm.lvs.opt }}"
  #  when: item.key == "opt"
  
  #- debug:
  #    msg: "{{ ansible_devices }}"


