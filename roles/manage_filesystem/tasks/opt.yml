---
- name: Extend Opt and Data logical volume
  lvol:
    vg: "{{ item.vg_name }}"
    lv: "{{ item.lv_name }}" 
    size: "{{ item.size }}"
    resizefs: "{{ item.resizefs }}"
  with_items: "{{ extend_data }}"
    #  when: vg_create.changed
    #  register: lv_extend
  ignore_errors: yes

