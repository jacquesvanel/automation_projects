---
#Get MongoDB Ops Manager Info
- name: Make API Call
  uri:
    url: "{{ ops_manger_host }}/groups/{{ project_id }}/"
    method: GET
    #    headers:
    #      Accept: "application/json"
    #      Content-Type: "application/json"
    #      Authorization: "Bearer {{ public_key }}"
    return_content: true
  register: ops_manager_info

- name: Print Response
  debug:
    var: ops_manager_info.json


