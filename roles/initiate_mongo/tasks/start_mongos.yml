---
- name: start mongos config 0
  shell: /usr/bin/mongos --quiet --config /etc/mongo/{{ env }}/mongoS0.cfg

- name: start mongos config 1
  shell: /usr/bin/mongos --quiet --config /etc/mongo/{{ env }}/mongoS1.cfg

- name: start mongos config 2
  shell: /usr/bin/mongos --quiet --config /etc/mongo/{{ env }}/mongoS2.cfg
