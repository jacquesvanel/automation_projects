systemLog:
  destination: file
  path: "/var/log/mongo/{{ env }}/{{ repname[3] }}-{{ repset[0] }}.log"
processManagement:
  fork: true 
  pidFilePath: "/var/run/mongo/mongod-{{ repname[3] }}-{{ repset[0] }}.pid"
security:
  keyFile: "/data/{{ env }}/keyfile.pem"
net:
  port: {{ configsvr_port }}
  bindIp: localhost,{{ ansible_hostname }}
  tls:
   mode: allowTLS
   certificateKeyFile: /var/lib/mongo/cert/KeyWCert.pem
   certificateKeyFilePassword: {{ tlspass }}
sharding:
  clusterRole: configsvr
replication:
  replSetName: {{ repname[3] }}_{{ env }}
storage:
  dbPath: /data/{{ env }}/config/{{ repset[0] }}
  wiredTiger:
    engineConfig:
      cacheSizeGB: {{ cachesize }}

