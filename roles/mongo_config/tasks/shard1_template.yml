---
# tasks file for mongo_config
- name: layout template for /etc/mongo/{{ env }}/mongod.conf on the shard1 server
  template:
    src:  shard1.conf.j2
    dest: "/etc/mongo/{{ env }}/shard1.conf"
    owner: root
    group: root
    mode: '0644'
  delegate_to: {{ shard1 }}


