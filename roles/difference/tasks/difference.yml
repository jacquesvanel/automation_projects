---
- name: get env
  shell: cat /etc/serverinfo.conf | grep Environment | sed 's/Environment=//g' | tr [:upper:] [:lower:]
  register: envi

- name: show env variable
  debug:
    msg: "{{ envi.stdout_lines }}"

- name: check if path exist
  shell:
    cmd: cat /etc/mongo/{{ envi.stdout_lines }}/rs_a.cfg | grep -i sharding
  register: replicatset

- debug:
    msg: "{{ replicatset.stdout_lines }}"  
