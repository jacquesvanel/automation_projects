---
# vars file for validate_mongo
#svc_ansible_automation: amkanda #svc_ansible_automation
#svc_ansible_automation_password: "Qwerty#12345px"
svc_ansible_automation: superUserAdmin #amkanda #svc_ansible_automation
svc_ansible_automation_password: "Asdfgh#12345" #"Qwerty#12345px"

disk_total: "{{ (item.size_total / 1000000000)| round(1, 'common') }}"
disk_available: "{{ (item.size_available / 1000000000) | round(1, 'common') }}"
disk_usage: "{{ ((item.size_total - item.size_available)/ 1000000000) | round(1, 'common') }}"
disk_percent: "{{ (((item.size_total - item.size_available)  / item.size_total) * 100)  | round(1, 'common') }}"
