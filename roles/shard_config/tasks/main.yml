---
# tasks file for shard_config
- include_tasks: shard1_template.yml
  when: inventory_hostname == 'shard1'

- include_tasks: shard2_template.yml
  when: hosts == 'shard2'

- include_tasks: shard3_template.yml
  when: hosts == 'shard3'
