---
- block:
  - name: add shards to cluster
    shell: mongo {{ configsvr_rsa }}.opr.afsiep.net:37018/admin /etc/mongo/{{ env }}/scripts/add_shards_to_cluster.js  -u amkanda -p Qwerty#12345px
    register: shell_result

  - debug:
      var: shell_result.stdout_lines
  when: (configsvr_rsa is defined) and (ansible_hostname == configsvr_rsa)
  ignore_errors: yes
