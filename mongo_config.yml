---
- name: Configure MongoDB
  hosts: "{{ configsvr }}"
  roles:
    - role: mongo_config

- name: configure shard1
  hosts: "{{ shard1 }}"
  roles:
    - role: shard_config

- name: configure shard2
  hosts: "{{ shard2 }}"
  roles:
    - role: shard_config      
