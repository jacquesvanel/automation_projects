---
- name: Configure MongoDB
  hosts: "{{ configsvr }}"
  roles:
    - role: mongo_config

- name: configure shard1
  hosts: "{{ shard1 }}"
  roles:
    - role: shard1_config

- name: configure shard2
  hosts: "{{ shard2 }}"
  roles:
    - role: shard2_config

- name: configure shard3
  hosts: "{{ shard3 }}"
  roles:
    - role: shard3_config
