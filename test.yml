---
 - name: running shell
   hosts: "{{target}}"
   gather_facts: no
   strategy: "{{ set_strategy | default(lookup('config', 'DEFAULT_STRATEGY')) }}"
   tasks:
    - name: run shell
      shell: "{{ run_command }}"
      args:
        warn: false

